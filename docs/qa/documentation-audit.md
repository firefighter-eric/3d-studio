# 文档核对记录

[文档总览](../README.md) · [设计理念](../design/overview.md) · [测试指南](../TESTING.md)

日期：2026-09-07。范围：当前工作树的 README、设计、使用、技术、制作、测试与发布文档。已有猎鹰重型 / 星舰的模型、动画、视频及其他实现改动不属于本次文档编辑范围。

> 后续发布调整（2026-09-07）：按要求忽略 MP4，改存本地 `output/videos/`，移除网页成片下载与原生视频入口，并同步更新本文档集。下文的原始验收记录仅描述当时的检查；发布验证以 PR 和对应部署记录为准。

## 原有缺口与处理

| 原有情况 | 本次处理 |
| --- | --- |
| README 随功能累加，缺少系统文档入口 | 重写简洁项目首页，新增按使用者与任务组织的文档总览 |
| 设计记录停留在三标签、四模型与概念图 | 新增当前四标签、统一库、设计原则、信息架构、视觉变量、交互与后续方向 |
| 操作说明和建模命令混在一起 | 分为使用指南、开发指南、资产指南和动画 / 视频指南 |
| 缺少架构与数据契约 | 记录模块关系、路由、搜索、资产 / 构型 / 实例、存储键与兼容、渲染和模拟职责 |
| SpaceX 仍有“重建两款”的旧命令说明 | 按当前三款与单模型导出参数更新，并记录相关视频再生依赖 |
| 历史测试数量容易被误读为当前结果 | 保留原证据与日期，增加历史范围说明，提供当前验收方法 |
| 缺少资产完整性与发布闭环规范 | 说明源代码、GLB、预览与 manifest 一起交付，MP4 本地保留；区分本地 / Preview / Production |

## 源码核对结果

从实际目录与分类导出确认 65 个模型、8 个品牌、21 种类型，来源分布为官网展示 12、外观重建 46、原创设计 7；61 个模型有 GLB 下载入口。DJI 有 19 款 / 26 构型；共享基地有 16 个基础实例、最多 12 个新增实例。

核对了 `package.json`、Codex 运行配置、`serve.mjs`、Vite / Vercel 配置、型号与资产清单、统一库 / 详情、存储读取、两套动画采样和视频导出脚本。文档明确保留当前限制：基础原创组件未单独导出 GLB；场景不保存 DJI 详情构型；Apple 预览没有专用 npm 脚本；SpaceX manifest 与其他家族 schema 不同；只有猎鹰重型具备本地 MP4 导出流程；Playwright / ffmpeg 属于制作工具。

## 文档检查

| 检查 | 结果 |
| --- | --- |
| Markdown 范围 | 22 份文档：新增 10 份，更新原有 12 份 |
| 导航与文件链接 | 261 处相对链接均可解析到存在的文件 / 目录；全部 22 份文档可从项目 README 到达 |
| Markdown 结构 | 25 个代码围栏成对；本地标题锚点引用按标题核对 |
| npm 命令 | 引用的 20 个 `npm run` 脚本均存在于 package.json；额外工具需求已分别说明 |
| 设计变量 | 8 个公共颜色变量与 styles.css 一致 |
| 数据清单 | 各品牌数量、DJI 构型数与共享目录一致；视频参数与交付清单一致，视频 / 模型指纹吻合 |
| 修改范围 | 对照编辑前快照，309 个既有非 Markdown 文件的 SHA-256 均未变化 |
| diff | `git diff --check` 通过，无新增尾随空白或冲突标记 |

核对方法为读取仓库文件和类型化目录，逐个解析 Markdown 相对链接、检查文档导航可达性、对照 package scripts / CSS 变量 / manifest，并比较编辑前后的非文档文件指纹。外部链接保留来源记录，不在上述内部链接通过数中。

## 验证范围说明

本次只编辑 Markdown，不重新生成模型或视频，不进行部署，也不把已有 QA 截图替换成当前截图。前一功能工作留有 97 项测试通过、TypeScript / 构建通过、Falcon Heavy 浏览器与原生 MP4 播放 / 下载通过的本地结果；本次读取这些结果了解上下文，没有将其写成本次新跑的测试或 Production 证明。

外部产品页面和线上部署本次未重新核对；来源日期按现有型号资料保留。本文记录文档维护结果，后续功能或发布仍按 [测试指南](../TESTING.md) 与 [发布指南](../RELEASE.md) 执行。
